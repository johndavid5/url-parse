/**
* Uses a finite-state machine to parse a url into host, port, and protocol...
*
* @returns a JSON object that looks something like this:
*     { host: "cnn.com", port: "80", protocol: "http" }
*
* @author: john.david.aynedjian@gmail.com
*          http://johndavidaynedjian.com
*          http://github.com/johndavid5
*/
function urlParse(url);

-----------
DIRECTIONS:
-----------


1. Install node and npm.

2. Run "npm install" at the command-line.
   This will install the necessary node modules in a local node_modules folder.

3. Run "npm test" at the command-line.
   This will run the unit tests found in the ./test folder.

4. Bon appetit!
